import 'package:flutter/material.dart';

class PhoneAccessoriesTab extends StatelessWidget {
  const PhoneAccessoriesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          'Phone Accessories'
      ),
    );
  }
}