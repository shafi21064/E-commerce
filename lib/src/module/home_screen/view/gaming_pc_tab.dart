import 'package:flutter/material.dart';

class GamingPcTab extends StatelessWidget {
  const GamingPcTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          'Gaming Pc'
      ),
    );
  }
}